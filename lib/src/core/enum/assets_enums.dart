enum AppImages {
  logo('logo');

  final String name;
  const AppImages(
    this.name,
  );
}

enum AppLotties {
  noNetwork('no_network'),
  succesful('succesful');

  final String name;
  const AppLotties(
    this.name,
  );
}
